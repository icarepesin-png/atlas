.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.env
atlas.db
# Le dossier data/ A LA RACINE reste local (cache prix, sauvegardes, logs).
# Le slash initial l'ancre a la racine: n'exclut PAS le package atlas/data/.
# Les noms de societes vivent dans config/ (versionne) pour le dashboard cloud.
/data/
